{
  "name": "@acme/tests",
  "private": true,
  "type": "module",
  "devDependencies": {
    "vitest": "^4.0.16",
    "set-cookie-parser": "^2.6.0",
    "wait-on": "^7.0.1",
    "@types/set-cookie-parser": "^2.4.7"
  },
  "scripts": {
    "test": "vitest run --reporter=dot"
  }
}
