# Enable hoisting for React Native compatibility with Metro bundler
# React Native + Metro have issues with pnpm's strict symlink structure
# because they expect peer dependencies to be accessible from node_modules
node-linker=hoisted

# Public hoist patterns for React Native ecosystem
public-hoist-pattern[]=*@react-native*
public-hoist-pattern[]=*metro*
public-hoist-pattern[]=*babel*
