# Hoist specific packages needed for React Native + Metro bundler
# This is more targeted than shamefully-hoist and shouldn't affect web builds
public-hoist-pattern[]=@babel/*
public-hoist-pattern[]=@react-native/*
public-hoist-pattern[]=react
public-hoist-pattern[]=react-native
public-hoist-pattern[]=react-native-*
public-hoist-pattern[]=metro*
public-hoist-pattern[]=invariant
public-hoist-pattern[]=nullthrows

# Hoist drizzle-orm to avoid duplicate type instances across packages
public-hoist-pattern[]=drizzle-orm
