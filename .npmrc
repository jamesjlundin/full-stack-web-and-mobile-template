# Enable shameful hoisting for React Native + Metro compatibility
# This hoists dependencies to root while keeping pnpm's .pnpm store structure
# (less aggressive than node-linker=hoisted)
shamefully-hoist=true
